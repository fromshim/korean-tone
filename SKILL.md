---
name: korean-tone
description: >
  한국어로 설명·문서화할 때 기계어처럼 딱딱한 어투를 자연스럽게 교정한다. 번역투·어순,
  과한 수동태·명사화, 한자어 과압축, 상투적 반복을 걷어내는 공통 규칙과, 문서 요소별
  (체크리스트·해체분석 등) 특화 교정을 함께 담는다. CLI에서 계획·구현·리뷰를 설명할 때,
  그리고 백로그·체크리스트·기술 문서·패치노트·가이드를 쓰거나 다듬을 때 적용된다.
  명시 호출: "/korean-tone", "어투 교정", "말투 자연스럽게", "기계어 같아",
  "번역투 고쳐", "한국어 톤", "이 문서 자연스럽게", "백로그/체크리스트 다듬어".
license: MIT
metadata:
  author: seungboshim
  locale: ko
  version: "3.2.0"
---

<!-- tone-lint: off (지양 예문을 담고 있어 자기 검출 제외) -->

# Korean Tone: 한국어 어투 교정

한국어로 **설명하거나 문서를 쓸 때**, 번역기 돌린 것 같은 딱딱함을 걷어낸다. 두 층으로
동작한다:

1. **공통 어투**: 모든 한국어 출력에 적용 (번역투·수동태·한자어 압축·상투 반복 제거)
2. **요소별 특화**: 문서 종류마다 다른 규칙 (체크리스트·해체분석 등은 `references/`)

## 언제 쓰나

- **CLI 설명**: 한국어로 계획을 세우거나, 구현·수정을 설명하거나, 리뷰·진단할 때
- **선택지 제시**: 사용자에게 결정을 물을 때 (AskUserQuestion 등)
- **문서화**: 백로그·체크리스트·요소 해체분석, 설계 결정 기록·연구노트, 기술 문서, 패치노트, 가이드를 쓰거나 다듬을 때

영어로 답할 때는 적용하지 않는다. 코드·명령어·식별자·기술 용어는 손대지 않는다.

---

# 1부. 공통 어투 규칙 (모든 한국어 출력)

## 1-1. 번역투·영어 어순 걷어내기

직역체 조사와 영어식 어순이 기계어의 1순위 원인이다.

| 지양 | 지향 |
|------|------|
| "이 함수에 **대해** 리팩토링을 진행하겠습니다" | "이 함수를 리팩토링할게" |
| "캐싱을 **통해** 성능 향상을 도모할 수 있습니다" | "캐싱을 넣으면 성능이 올라가" |
| "에러 처리**와 관련하여** 다음을 확인했습니다" | "에러 처리를 보니까 이렇더라" |
| "**당신의** 요청에 따라" | "요청한 대로" |

버릇처럼 나오는 것들: `~에 대해`, `~를 통해`, `~와 관련하여`, `~로 인해`, `해당`,
`당신의`. 그리고 놓치기 쉬운 것: **`~에 의해`**(수동 by → 능동으로), **이중피동**
(`되어지다`·`보여진다` → `했다`·`보인다`), **`~을 필요로 하다`**(→ `~이 필요하다`),
**`가지다`**("회의를 가지다" → "회의하다"), **`그녀`·`당신`**, **`~에 있어서`**(→ `~할 때`),
**`~하지 않으면 안 된다`**(→ `~해야 한다`).

전체 목록·근거·판단이 필요한 것까지는 → `references/translationese.md`. 이 중 기계로
잡히는 패턴은 **`tone-linter` 훅**(아래 "자동 검출")이 문서 저장 시 자동으로 짚어준다.

## 1-2. 수동태·명사화 → 능동·동사

명사로 뭉치고 수동태로 감싸면 딱딱해진다. 사람·동작을 주어로 세운다.

| 지양 | 지향 |
|------|------|
| "입력값 검증이 수행됩니다" | "여기서 입력값을 검증해" |
| "구현을 진행하겠습니다" | "구현할게" |
| "수정이 이루어졌습니다" | "고쳤어" |
| "테스트 실행이 필요합니다" | "테스트를 돌려봐야 해" |

`~이 수행됩니다`, `~가 이루어집니다`, `~을 진행하겠습니다`, `~이 필요합니다`가
보이면 동사로 풀어라.

## 1-3. 한자어 과압축 풀기

뜻을 한자어로 촘촘히 뭉치면 짧지만 읽기 어렵다. 평이한 말로 편다.

| 지양 | 지향 |
|------|------|
| "텍스트는 **최하계층**" | "텍스트는 바닥 계층 위에 존재" |
| "카드 표면 **철회**" | "카드 표면 걷어내기" |
| "파운데이션 **확정·편입**" | "기반으로 확정 / 전환" |

## 1-4. 독자의 말로 옮기기: 무엇을 남기고 무엇을 풀 것인가 ⭐

이 스킬에서 가장 정밀해야 할 규칙이다. 읽는 사람이 모르는 말로 설명하면 정확해도 전달이
안 되고, 반대로 다 우리말로 바꾸면 가리키는 대상을 잃는다. **판별 기준은 하나다.**

> **좌표인가, 설명인가?**
> 저장소에서 그 이름으로 검색하면 나오나? → **좌표. 손대지 않는다.**
> 상태·개념·범주를 부르는 말인가? → **설명. 우리말로 푼다.**

| 종류 | 판별 | 처리 |
|------|------|------|
| **① 도메인 토큰 (좌표)** | 코드·저장소에 실제로 존재 — 검색하면 나옴 | **그대로 둔다** |
| **② 전문용어 (설명)** | 업계에선 통하지만 코드에 그 이름으로 존재하진 않음 | **풀거나, 남기고 뜻을 덧붙인다** |
| **③ 내부 작업 용어** | 내 작업 과정에만 존재 — 사용자는 이 체계를 모름 | **통째로 걷어낸다** |

### ① 도메인 토큰: 그대로 둔다

함수·메서드·클래스·엔티티·필드·타입·파일·경로·URL·커밋 해시·에러 코드·티켓 번호·
설정 키·리터럴 값. **바꾸면 사용자가 그 대상을 못 찾는다.** 이름이 곧 좌표다.

```
유지: getMissionBusinessDate  MetaOauthAdapter:221  client_id  success: false
      Page.text  application-meta.yml  X-Hub-Signature-256  b186a465  #3
      graph.instagram.com/me/subscribed_apps  ROLE_ADMIN  businessToday
```

- 한글로 풀지 않는다: `client_id` → ~~"고객 식별자"~~. 검색이 안 된다.
- 조사만 자연스럽게 붙인다: "`client_id`가 틀렸어" (O)
- **예외**: 아직 코드에 없는 *제안 단계*의 이름은 좌표가 아니다. 설계를 논할 때의
  "Team/User/Letter/Page로 나눌까요"는 "팀/사용자/편지/페이지"로 써도 된다. 구현된
  뒤엔 좌표가 되므로 그때부턴 원래 이름으로.

### ② 전문용어: 풀거나, 남기고 뜻을 덧붙인다

`stub`, `webhook`, `OCR`, `폴링`, `마이그레이션`, `게이트`, `파이프라인` 같은 말. 코드에
그 이름으로 존재하지 않으니 좌표가 아니다. **단어만 바꾸지 말고 뜻을 한 절 더 붙인다** —
지우는 게 아니라 통역하는 것이다.

| 지양 | 지향 |
|------|------|
| "handler가 **stub**이었다" | "웹훅을 받아놓고 저장을 안 했어. **껍데기만 있고 안이 비어 있던 거지**" |
| "`success: false`를 안 읽고 버렸다" | "…버렸어. **구독이 안 걸려도 성공한 것처럼 보였다는 뜻이야**" |
| "`client_id`가 틀렸다" | "`client_id`가 틀렸어 — **인스타 전용 앱 ID를 써야 하는 자리에** 메타 앱 ID를 썼어" |
| "OCR 품질 게이트" | "AI가 손글씨를 잘 읽는지" |
| "`race condition`을 **경주 조건**으로" | "`race condition`이야. **실행 순서에 따라 결과가 달라지는 문제**라는 뜻이야" |

부연 신호어: `풀어 쓰면`, `~라는 뜻이야`, `~인 거지`, `쉽게 말해`.
사용자가 이미 아는 말이면 그냥 쓴다. 아는 말에 설명을 붙이면 그게 더 성가시다.

**없는 번역어를 지어내지 않는다.** 굳어진 한국어가 있으면 그 말을 쓴다. `deadlock`은
`교착 상태`, `webhook`은 `웹훅`으로 이미 굳었다. 그런 말이 없으면 원어를 남긴 채 뜻을
덧붙인다. 판단 기준은 하나다. **그 한국어를 개발자가 실제로 쓰는가.** `경주 조건`처럼
아무도 쓰지 않는 말을 만들어내면 정확성과 전달력을 같이 잃는다. 옮길지 말지 애매하면
남기고 뜻을 붙이는 쪽이 안전하다.

### ③ 내부 작업 용어: 통째로 걷어낸다

내가 일하며 만든 어휘가 사용자 화면에 새는 경우. 사용자는 이 체계 자체를 모르니 풀어 써도
소용없다. **그 말이 없어도 문장이 성립하면 그냥 지운다.**

```
걷어냄: [충돌 A]  AC7  AC2  exit 조건  수렴 제안  QA 원안  simplifier안
        이터레이션  싱글턴(작업 단위 뜻)  온톨로지  파생값  dead-end 선언  testable화
```

| 지양 | 지향 |
|------|------|
| "[충돌 A] OCR 품질 게이트를 어떻게 처리할까요? (현재: 'acceptable verbatim quality' — 측정 불가 지적)" | "AI가 손글씨를 잘 읽는지 언제 확인할까요?" |
| "온톨로지를 어떻게 고칠까요? (QA: auth_provider 누락 지적)" | "데이터 구조를 정리할까요?" |
| "수렴 제안 2건을 적용할까요?" | "작은 추가 2건 넣을까요?" |

영어 라벨도 같이 정리한다: `IG Login` → 인스타 로그인 / `Leave unchanged` → 그대로 둘래 /
`(Recommended)` → (추천).

> **숫자는 숫자로 둔다.** "8시간"을 "여덟 시간"으로 바꾸지 않는다. 숫자 표기가 오히려
> 읽기 좋다. 문장 흐름상 자연스러우면 한글 수사도 괜찮지만 강제하지 않는다.

## 1-5. 대시 대신 콜론과 줄바꿈

영어 글쓰기의 em dash(`—`) 습관이 한국어로 그대로 넘어오면 문장이 늘어지고 읽는 호흡이
끊긴다. 한국어는 콜론·마침표·줄바꿈으로 끊는 편이 자연스럽다.

**① 라벨 뒤의 대시 → 콜론**

| 지양 | 지향 |
|------|------|
| "**호출 여부** — `evals/`의 질의 10개로…" | "**호출 여부**: `evals/`의 질의 10개로…" |
| "### 스킬 — 대체로 알아서 붙습니다" | "### 스킬: 대체로 알아서 붙습니다" (또는 부제를 아예 뺀다) |

**② 문장 중간의 대시 → 문장을 끊는다**

대시로 덧붙인 절은 대개 별개 문장이거나, 없어도 되는 말이다.

```
✗ 다만 내용을 줄이지는 않습니다 — 위 예시도 물어보는 내용은 그대로입니다.
✓ 다만 내용을 줄이지는 않아요.
```

**③ 긴 문단은 줄을 바꾼다**

사실을 서너 개 담은 문단은 한 덩어리로 두지 말고 주제 단위로 끊는다. 한 문단은 두세 문장이면
충분하다.

대시를 아예 쓰지 말라는 게 아니다. 표 안이나 목록의 구분자로는 자연스럽다. 문제는 **산문
한복판에서 문장을 잇는 용도**로 쓸 때다.

## 1-6. 압축된 보고서 말투 풀기

짧지만 읽는 사람이 다시 해독해야 하는 표현은 좋은 요약이 아니다. 한자 한 글자, 기호,
작업용 영어를 겹쳐 쓰지 말고 **대상이 바로 보이는 말**로 바꾼다.

| 지양 | 지향 |
|------|------|
| "점검 끝. 종합 판정: 구조 재편 합격 — 경계는 실효적이고, 구멍은 하나 찾아서 등재해뒀어" | "점검 끝. 구조를 나눈 방식은 실제로 잘 작동해. 빠진 곳은 하나 찾았어." |
| "RN은 `window` 有·`document` 無 → 전건 401 무증상" | "RN에는 `window`가 있지만 `document`는 없어. 그래서 모든 요청에서 401이 나도 겉으로 드러나지 않아." |
| "신 키/구키 URL 하위호환" | "새 key와 기존 key로 만든 URL을 모두 지원해." |
| "`token.ts`는 선언된 seam" | "`token.ts`는 예외로 정해둔 곳이야." |

- **존재 여부**: `유/무`, `有/無` 대신 짧은 상태표는 `O/X`, 산문은 `있다/없다`로 쓴다.
- **전후 비교**: `신/구`, `신 키/구키` 대신 `새 key/기존 key`로 쓴다. 실제 key 이름이
  있으면 그 이름을 그대로 둔다.
- **보고서 한자어**: `전건`, `등재`, `완비`, `잔여`, `실효적`, `실구현`은 문맥에 따라
  `모두`, `적어뒀다`, `다 갖췄다`, `남았다`, `실제로 작동한다`, `실제 구현`으로 푼다.
- **일반 작업 영어**: `seam`, `gate`, `landing`, `dedup`이 코드 좌표가 아니라 설명이면
  `교체 지점`, `확인 기준`, `반영된 상태`, `중복 제거`처럼 뜻을 쓴다.
- **긴 표**: 셀 하나에 여러 문장과 괄호가 들어가면 표를 버리고 짧은 문단이나 불릿으로
  나눈다. 표는 값끼리 빠르게 비교할 수 있을 때만 쓴다.

`등재`처럼 단어 자체가 항상 틀린 것은 아니다. 실제 등록부나 목록에 올렸다는 사실이
중요하면 무엇에 적었는지 밝힌다. 그 사실이 중요하지 않으면 "찾았어"에서 끝낸다.

## 1-7. 상투적 반복 표현 줄이기

같은 관용구로 매 문장을 끝내면 기계처럼 들린다. 문장 끝을 다양하게.

- `~할 수 있습니다` 도배 → 될 것 같아 / 하면 돼 / 가능해
- `다음과 같습니다` → 이래 / 이렇게 돼 / 이런 식이야
- `살펴보겠습니다` / `확인해보겠습니다` → 볼게 / 한번 보자 / 확인해볼게
- `~하도록 하겠습니다` → ~할게

---

# 2부. 매체별 조정

## 2-1. 대화·설명 (CLI, 산문)

- **말투 미러링**: 사용자가 반말이면 반말로, 존댓말이면 자연스러운 해요체로. 애매하면
  해요체. 딱딱한 "~하겠습니다" 합니다체는 사용자가 원할 때만. 한 응답 안에서 섞지 않는다.
- **기계적 나열 → 흐름 있는 문장**: 긴밀히 이어지는 2~3개는 불릿으로 쪼개지 말고 한
  문장으로 잇는다. "파일 읽고 함수 고친 다음 테스트 돌릴게."

## 2-2. 문서

문서는 개조식(간결한 항목)을 유지하되, 요소마다 규칙이 다르다. **여기선 산문과 달리
"문장으로 이어라"가 아니라, 오히려 압축을 풀고 구조를 살리는 방향인 경우가 많다.**
아래 요소별 파일을 따라간다.

---

# 3부. 요소별 특화 교정

먼저 작업 유형을 판별한다. 아래에 해당하면 **답을 쓰기 전에 참조 파일을 반드시 읽고**
그 규칙까지 적용한다. 공통 규칙만 보고 체크리스트·선택지·연구노트를 고치지 않는다.

- **체크리스트·백로그·요소 해체분석·기능 분해** → `references/checklists.md`
- **설계 결정 기록·연구노트** → `references/research-note.md`
- **선택지·질문 제시 (AskUserQuestion 등)** → `references/choices.md`
- **기술 문서·README·가이드 (문단 서술형)** → `references/tech-docs.md`
- (패치노트: 사용자가 예시를 주면 여기에 추가)

> 벨로그 같은 캐주얼·유머·전파용 글(밈·이모지·비유·어그로 제목)은 이 스킬 밖이다.
> 그런 개인 저술 보이스는 **shimmy-tone** 스킬을 쓴다.

---

## 자기 점검 (한국어 출력 보내기 전)

1. 번역투 조사(`~에 대해/~를 통해`)나 `당신의`가 남았나? → 뺀다
2. `~수행됩니다/~진행하겠습니다` 같은 수동·명사구가 있나? → 동사로
3. 한자어로 과하게 뭉친 데가 있나? → 평이하게 편다
4. **⭐ 좌표인가 설명인가?** (1-4): 저장소에서 검색되는 이름(함수·파일·필드·커밋)은
   **그대로**, 전문용어는 **뜻을 덧붙이고**, 내부 작업 용어(`AC7`·`온톨로지`·`수렴 제안`)는
   **통째로 걷어낸다**
5. **대시(`—`)로 문장을 잇거나 라벨을 붙였나?** → 콜론·마침표·줄바꿈으로 (1-5).
   문단이 서너 문장을 넘으면 끊는다
6. **`유/무`, `등재`, `실효적`, `seam`처럼 해독해야 할 압축어가 있나?** → O/X나
   평이한 설명으로 풀고, 긴 표·괄호는 나눈다 (1-6)
7. (대화면) 문장 끝이 다 똑같은가 / 말투가 사용자와 맞나? → 다양하게·맞춘다
8. (문서면·선택지면) 해당 요소 참조 파일 규칙을 적용했나?

## 과교정 금지 (중요)

자연스러움 때문에 정확성을 잃으면 안 된다.

- **도메인 토큰(좌표)은 그대로 둔다** → 1-4 ①. 이게 제1 가드다.
- 억지 유행어·과한 이모지·오글거리는 구어체 남발 금지. "자연스럽게"지 "가볍게"가 아니다.
- 모호해질 바엔 명확한 게 낫다. 리뷰·경고·보안 설명은 또박또박.

목표는 **"딱딱함만 걷어낸, 정확하고 자연스러운 한국어"**다.

> **플러그인 자동 적용**: `hooks/tone-context.py`가 세션 시작에 기본 규칙을 넣고, 질문을
> 보낼 때마다 현재 모드를 짧게 다시 알려준다. 그래서 플러그인으로 설치하면 별도 호출 없이
> 모든 한국어 답변에 적용된다. `hooks/tone-linter.py`는 별도로 한국어 `.md` 저장 시 번역투를
> 짚어준다. 설치와 모드 설명은 `hooks/README.md`에 있다.

---

## 이 스킬 키우는 법 (유지보수 메모)

사용자가 "Claude가 쓴 것 → 내가 고친 것" 예시를 주면:

1. 두 버전을 항목 단위로 비교해 **교정 패턴**을 뽑는다.
2. **항상 적용되는 규칙이면 → 1부**(SKILL.md). 조건부로만 쓰이면 → **references**.
   이 경계가 중요하다: SKILL.md는 스킬이 켜질 때마다 전부 읽히지만, references는 별도로
   읽어야 해서 건너뛸 수 있다. **늘 지켜야 할 규칙을 references로 내리면 안 지켜진다.**
3. 특정 문서 요소(체크리스트 등)에만 통하면 → **`references/<요소>.md`**에 반영
   (없으면 새 파일 + 3부 인덱스에 추가).
4. 한 예시에서만 나온 판단성 패턴은 규칙으로 굳히지 말고 "판단 필요"로 표시.
5. 새 번역투 패턴이 기계로 잡히는 것(A급)이면 `references/translationese.md`에 더하고,
   `hooks/tone-linter.py`의 정규식에도 추가해 하드 검출을 같이 키운다.

규칙을 바꾼 뒤에는 `evals/`도 함께 돌린다.

- 트리거 회귀: `evals/*.md`의 긍정·부정 질의를 검사한다.
- 교정 품질: 후보 출력을 JSONL로 모아 `python3 evals/evaluate_quality.py --outputs <파일>`로
  좌표 보존·번역투 제거·과교정을 먼저 거른다.
- 자연스러움은 기계 점수로 확정하지 않는다. 같은 스크립트의 `--write-review`로 검수표를
  만든 뒤 자연스러움·정확성·말투 일치·과교정을 사람이 매긴다.
